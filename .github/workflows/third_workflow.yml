name: Third Workflow
on: [push]

jobs: 
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: List Files
        run: ls -a
      - uses: actions/checkout@v4
      - name: List Files after copy
        run: ls -a
  
  